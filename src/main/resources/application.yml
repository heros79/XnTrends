server:
  port: 8080

spring:
  #Spring JDBC configuration
  datasource:
    url:      jdbc:mysql://127.0.0.1:3306/xntrends
    username: root
    password: root

  #  url:      jdbc:oracle://127.0.0.1:3306/xntrends
  #  username:
  #  password:

    #Spring Data JPA configuration
  jpa:
    database-platform: org.hibernate.dialect.MySQL55Dialect
  # database-platform: org.hibernate.dialect.Oracle10gDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  messages:
    basename: validation

logging:
  file: log.txt